# Origin'ы фронта (откуда браузер шлёт запросы) — для CORS сервера.
# Можно несколько через запятую: http://localhost,http://localhost:5173
CORS_ORIGINS=http://localhost

# URL backend-API, на который клиент шлёт запросы
VITE_API_URL=http://api.localhost
